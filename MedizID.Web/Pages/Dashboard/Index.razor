@page "/dashboard"
@using MedizID.Web.Services
@inject IAuthenticationService AuthService
@inject NavigationManager Navigation
@inject IApiService ApiService

@if (!isAuthenticated)
{
    <div class="flex items-center justify-center h-screen">
        <div class="text-center">
            <p class="text-gray-600">Checking authentication...</p>
        </div>
    </div>
}
else
{
    <div class="min-h-screen bg-gray-100">
        <!-- Navigation Bar -->
        <nav class="bg-white shadow">
            <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
                <div class="flex justify-between items-center h-16">
                    <h1 class="text-2xl font-bold text-blue-600">mediz.id</h1>
                    <div class="flex items-center gap-4">
                        <span class="text-gray-700">Welcome, @currentUser?.FullName</span>
                        <button @onclick="HandleLogout" class="bg-red-600 hover:bg-red-700 text-white px-4 py-2 rounded">
                            Logout
                        </button>
                    </div>
                </div>
            </div>
        </nav>

        <!-- Main Content -->
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4 mb-8">
                <!-- Dashboard Cards -->
                <div class="bg-white rounded-lg shadow p-6">
                    <div class="flex items-center justify-between">
                        <div>
                            <p class="text-gray-600 text-sm">Total Patients</p>
                            <p class="text-3xl font-bold text-blue-600">---</p>
                        </div>
                        <div class="text-4xl text-blue-200">👥</div>
                    </div>
                </div>

                <div class="bg-white rounded-lg shadow p-6">
                    <div class="flex items-center justify-between">
                        <div>
                            <p class="text-gray-600 text-sm">Today's Appointments</p>
                            <p class="text-3xl font-bold text-green-600">---</p>
                        </div>
                        <div class="text-4xl text-green-200">📅</div>
                    </div>
                </div>

                <div class="bg-white rounded-lg shadow p-6">
                    <div class="flex items-center justify-between">
                        <div>
                            <p class="text-gray-600 text-sm">Pending Reviews</p>
                            <p class="text-3xl font-bold text-yellow-600">---</p>
                        </div>
                        <div class="text-4xl text-yellow-200">📋</div>
                    </div>
                </div>

                <div class="bg-white rounded-lg shadow p-6">
                    <div class="flex items-center justify-between">
                        <div>
                            <p class="text-gray-600 text-sm">Active Alerts</p>
                            <p class="text-3xl font-bold text-red-600">0</p>
                        </div>
                        <div class="text-4xl text-red-200">🔔</div>
                    </div>
                </div>
            </div>

            <!-- Quick Actions -->
            <div class="bg-white rounded-lg shadow p-6">
                <h2 class="text-xl font-bold mb-4">Quick Actions</h2>
                <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4">
                    <a href="/patients" class="bg-blue-600 hover:bg-blue-700 text-white p-4 rounded text-center font-medium">
                        View Patients
                    </a>
                    <a href="/patients/create" class="bg-green-600 hover:bg-green-700 text-white p-4 rounded text-center font-medium">
                        Add New Patient
                    </a>
                    <a href="/appointments" class="bg-purple-600 hover:bg-purple-700 text-white p-4 rounded text-center font-medium">
                        View Appointments
                    </a>
                    <a href="/medical-records" class="bg-indigo-600 hover:bg-indigo-700 text-white p-4 rounded text-center font-medium">
                        Medical Records
                    </a>
                </div>
            </div>
        </div>
    </div>
}

@code {
    private bool isAuthenticated = false;
    private AuthUser? currentUser;

    protected override async Task OnInitializedAsync()
    {
        isAuthenticated = await AuthService.IsAuthenticatedAsync();
        if (!isAuthenticated)
        {
            Navigation.NavigateTo("/login");
            return;
        }

        currentUser = await AuthService.GetCurrentUserAsync();
    }

    private async Task HandleLogout()
    {
        await AuthService.LogoutAsync();
        Navigation.NavigateTo("/login");
    }
}
