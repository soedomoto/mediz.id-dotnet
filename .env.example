# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=mediz_db
DB_USER=mediz_user
DB_PASSWORD=mediz_password

# PostgreSQL Connection String (constructed from above if not set)
# DATABASE_URL=Host=localhost;Port=5432;Database=mediz_db;Username=mediz_user;Password=mediz_password;

# JWT Settings
JWT_SECRET_KEY=your-super-secret-key-at-least-32-characters-long-for-hmac256
JWT_ISSUER=mediz.id
JWT_AUDIENCE=mediz.id-api
JWT_EXPIRATION_MINUTES=1440

# OpenAI Settings
OPENAI_API_KEY=your-openai-api-key-here

# Application Settings
ASPNETCORE_ENVIRONMENT=Development
ASPNETCORE_URLS=http://+:5000
